FILE(GLOB spreg_srcs ${CMAKE_CURRENT_LIST_DIR}/*.cpp)
add_library(spreg ${spreg_srcs})
add_dependencies(spreg pcre)
target_link_libraries(spreg pcre)
include_directories(${CMAKE_CURRENT_LIST_DIR}/include)
