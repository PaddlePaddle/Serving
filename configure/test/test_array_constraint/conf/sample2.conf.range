struct sample_t
{
    string name = default("default");
    string info = default("");
    int32_t ratio = default(50);
};

struct sample_level_t
{
    string default_name = default("default");
    sample_t sample[];
};

sample_level_t sample_level[];
