struct Person{
	int32_t age = range(0, 200);
	string name = length(32);
};

Person com[4];
string name;
int32_t Putian = range(-4, 18);
int32_t num[3] = range(0,10);
string ip[1] = ip();

string reg[4] = regexp("[A-Za-z]*"), length(10);
